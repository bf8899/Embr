import { Landing } from "@/components/landing";

// The marketing page, kept for pitching (investors, ads, campaigns). The root
// route is the product itself (public browse); this lives at /welcome.
export default function WelcomePage() {
  return <Landing />;
}
